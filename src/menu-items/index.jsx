// project import
import dashboard from './dashboard';
import pages from './page';
import support from './support';
import features from './features';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, pages, features, support]
};

export default menuItems;
